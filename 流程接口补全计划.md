# 流程接口补全实施计划

## 实施进度跟踪

**更新时间**: 2025-11-12

### 已完成接口 (7/10 - 70%) ✅

| 接口 | 优先级 | 状态 | 完成时间 |
|------|--------|------|----------|
| GetJourneyBySN | ⭐⭐⭐⭐⭐ | ✅ 已完成 | 2025-11-12 |
| GetJourneyAssignments | ⭐⭐⭐⭐⭐ | ✅ 已完成 | 2025-11-12 |
| UpdateJourneyStatus | ⭐⭐⭐⭐⭐ | ✅ 已完成 | 2025-11-12 |
| GetJourneyDetail | ⭐⭐⭐⭐ | ✅ 已完成 | 2025-11-12 |
| GetFlowDetail | ⭐⭐⭐ | ✅ 已完成 | 2025-11-12 |
| **GetUserAssignments** | ⭐⭐⭐ | ✅ **新完成** | 2025-11-12 |
| **GetProposedJourneys** | ⭐⭐⭐ | ✅ **新完成** | 2025-11-12 |

**新增特性**：
- ✅ **性能优化**：GetUserAssignments 自动补充 flow_id 和 flow_title（API调用减少93%+）
- ✅ **配置化缓存**：Flow信息缓存TTL可配置（默认3600秒），支持自定义
- ✅ **本地用户ID支持**：Engine层接收本地用户ID，自动转换为远程用户ID

### 待实现接口 (3/10)

| 接口 | 优先级 | 状态 | 说明 |
|------|--------|------|------|
| SearchJourneys | ⭐⭐⭐ | 📋 待实现 | 流程记录搜索 |
| GetJourneyMoments | ⭐⭐⭐ | 📋 待实现 | 获取流程审批历史 |
| GetCurrentProcessingUsers | ⭐⭐ | 📋 待实现 | 获取当前流程任务的处理者 |
| AbortJourney | ⭐⭐ | 📋 待实现 | 终止流程任务 |

### 实施要点

- ✅ 创建 `core/journey.go` 存放 Journey 相关领域模型
- ✅ 遵循三层架构：Engine → Internal → Core
- ✅ 使用 `errors.Is` 进行错误检查
- ✅ HTTP 404 自动映射为业务错误
- 📝 后续接口可复用已实现的结构体和模式

---

## 一、背景与目标

### 已解决问题 ✅
通过实现 `GetJourneyBySN` 和 `GetJourneyAssignments`，调用链已打通：
- ✅ 从流程编号 → 获取 journeyID（`GetJourneyBySN`）
- ✅ 从 journeyID → 获取 assignmentID（`GetJourneyAssignments`）
- ✅ 更新流程状态（`UpdateJourneyStatus`，已重构为租户模式）

### 实施目标
1. ✅ **已完成**：核心流程更新调用链（通过 5 个接口实现）
2. ✅ **已完成**：核心工作台接口（GetUserAssignments + GetProposedJourneys，含性能优化）
3. 📋 **待完成**：4 个辅助接口（搜索、审批历史、当前处理人、终止流程）
4. ✅ **架构规范**：所有接口遵循三层架构 + 租户模式 + 集中式验证 + 配置化

---

## 二、性能优化方案：映射库+缓存 ✅

### 2.1 实施状态

✅ **已完成** (2025-11-12)
- ✅ 映射库查询（批量查询 Skylark 远程数据库）
- ✅ Redis 缓存（可配置 TTL，默认1小时）
- ✅ 批量优化（减少 API 调用 93%+）
- ✅ 容错机制（失败不影响主流程）

### 2.2 配置说明

**缓存配置**（`internal/flows/config.go`）：
```go
type Config struct {
    // FlowInfoCacheTTL Flow信息缓存过期时间（秒）
    // 默认值：3600（1小时）
    // 建议范围：1800-7200（30分钟到2小时）
    FlowInfoCacheTTL int
}
```

**初始化示例**：
```go
// 使用默认配置（TTL=3600秒）
flows.NewSkylarkFlowRegistry(&flows.Config{}, ...)

// 自定义缓存TTL
flows.NewSkylarkFlowRegistry(&flows.Config{
    FlowInfoCacheTTL: 7200, // 2小时
}, ...)
```

### 2.3 问题分析

`GET /api/v4/yaw/flows/user_assignments.json` 返回的 assignment 列表中只有 `journey_id`，缺少 `flow_id` 和 `flow_title`，导致前端需要额外请求才能显示流程名称。

### 2.4 解决方案架构

采用**三层优化策略**：映射库 + 缓存 + 批量查询

```
┌─────────────────────────────────────────────────────────────┐
│  用户请求：获取待处理列表（100条assignment）                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 调用 Skylark API 获取 assignment 列表               │
│  返回：[{id, journey_id, vertex_id, ...}, ...]              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 提取所有唯一的 journey_id                            │
│  结果：[3990, 3887, 3445, ...]（假设50个不同的journey）       │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 3: 批量查询 journey_id → flow_id 映射                  │
│  方案A（推荐）：查询本地 PostgreSQL                           │
│    SELECT journey_id, flow_id FROM journeys                 │
│    WHERE journey_id IN (3990, 3887, 3445, ...)              │
│  方案B：查询 Skylark 数据库（如果有只读副本）                  │
│  结果：{3990: 135, 3887: 135, 3445: 142, ...}               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 提取所有唯一的 flow_id                              │
│  结果：[135, 142, 156]（假设只有3个不同的flow）                │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 5: 批量查询 flow_id → flow_title（Redis 缓存）         │
│  MGET skylark:flow:135 skylark:flow:142 skylark:flow:156    │
│  命中：{135: "打卡成功", 142: "请假申请"}                     │
│  未命中：[156]                                               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 6: 批量调用 Skylark API 获取未命中的 flow 信息         │
│  GET /api/v4/yaw/flows/156                                  │
│  结果：{id: 156, title: "报销流程", ...}                     │
│  并回写缓存：SET skylark:flow:156 "{...}" EX 3600           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 7: 合并数据，返回完整的列表                             │
│  [{id, journey_id, flow_id, flow_title, ...}, ...]          │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 实现细节

#### 2.3.1 数据库查询优化

**选择方案A**：查询本地 PostgreSQL 的 `journeys` 表

```go
// internal/flows/handler.go
func (f *skylarkFlowRegistry) enrichAssignmentsWithFlowInfo(
    ctx context.Context,
    assignments []*core.Assignment,
) error {
    // Step 1: 提取所有唯一的 journey_id
    journeyIDs := make([]int64, 0, len(assignments))
    journeyIDSet := make(map[int64]bool)
    for _, a := range assignments {
        if !journeyIDSet[a.JourneyID] {
            journeyIDs = append(journeyIDs, a.JourneyID)
            journeyIDSet[a.JourneyID] = true
        }
    }

    // Step 2: 批量查询 journey_id → flow_id 映射
    journeyToFlowMap, err := f.getJourneyFlowMapping(ctx, journeyIDs)
    if err != nil {
        return err
    }

    // Step 3: 提取所有唯一的 flow_id
    flowIDs := make([]int64, 0)
    flowIDSet := make(map[int64]bool)
    for _, flowID := range journeyToFlowMap {
        if !flowIDSet[flowID] {
            flowIDs = append(flowIDs, flowID)
            flowIDSet[flowID] = true
        }
    }

    // Step 4: 批量获取 flow 信息（带缓存）
    flowInfoMap, err := f.batchGetFlowInfo(ctx, flowIDs)
    if err != nil {
        return err
    }

    // Step 5: 合并数据
    for _, a := range assignments {
        flowID, ok := journeyToFlowMap[a.JourneyID]
        if !ok {
            continue
        }
        flowInfo, ok := flowInfoMap[flowID]
        if !ok {
            continue
        }
        a.FlowID = flowID
        a.FlowTitle = flowInfo.Title
    }

    return nil
}
```

#### 2.3.2 映射库实现

```go
// internal/flows/mapping.go

// getJourneyFlowMapping 批量查询 journey_id → flow_id 映射
func (f *skylarkFlowRegistry) getJourneyFlowMapping(
    ctx context.Context,
    journeyIDs []int64,
) (map[int64]int64, error) {
    if len(journeyIDs) == 0 {
        return make(map[int64]int64), nil
    }

    // 方案A：查询本地 PostgreSQL（推荐）
    // 假设项目已配置了到 Skylark PostgreSQL 的只读连接
    query := `
        SELECT id as journey_id, flow_id
        FROM journeys
        WHERE id = ANY($1)
    `

    rows, err := f.db.QueryContext(ctx, query, pq.Array(journeyIDs))
    if err != nil {
        return nil, fmt.Errorf("查询journey映射失败: %w", err)
    }
    defer rows.Close()

    result := make(map[int64]int64, len(journeyIDs))
    for rows.Next() {
        var journeyID, flowID int64
        if err := rows.Scan(&journeyID, &flowID); err != nil {
            return nil, err
        }
        result[journeyID] = flowID
    }

    return result, rows.Err()
}
```

#### 2.3.3 缓存层实现

```go
// internal/flows/cache.go

type FlowInfo struct {
    ID    int64
    Title string
}

// batchGetFlowInfo 批量获取 flow 信息（带缓存）
func (f *skylarkFlowRegistry) batchGetFlowInfo(
    ctx context.Context,
    flowIDs []int64,
) (map[int64]*FlowInfo, error) {
    if len(flowIDs) == 0 {
        return make(map[int64]*FlowInfo), nil
    }

    // Step 1: 批量查询 Redis 缓存
    cacheKeys := make([]string, len(flowIDs))
    for i, flowID := range flowIDs {
        cacheKeys[i] = fmt.Sprintf("skylark:flow:%d", flowID)
    }

    cachedValues, err := f.cache.MGet(ctx, cacheKeys)
    if err != nil {
        logx.Errorf("Redis MGET 失败: %v", err)
        // 继续执行，视为全部未命中
        cachedValues = make([]string, len(flowIDs))
    }

    // Step 2: 整理缓存命中和未命中的 flow_id
    result := make(map[int64]*FlowInfo, len(flowIDs))
    missedFlowIDs := make([]int64, 0)

    for i, flowID := range flowIDs {
        if cachedValues[i] != "" {
            // 缓存命中
            var flowInfo FlowInfo
            if err := json.Unmarshal([]byte(cachedValues[i]), &flowInfo); err == nil {
                result[flowID] = &flowInfo
            } else {
                missedFlowIDs = append(missedFlowIDs, flowID)
            }
        } else {
            // 缓存未命中
            missedFlowIDs = append(missedFlowIDs, flowID)
        }
    }

    // Step 3: 批量调用 API 获取未命中的 flow 信息
    if len(missedFlowIDs) > 0 {
        for _, flowID := range missedFlowIDs {
            flowInfo, err := f.fetchFlowInfoFromAPI(ctx, flowID)
            if err != nil {
                logx.Errorf("获取 flow %d 信息失败: %v", flowID, err)
                continue
            }

            result[flowID] = flowInfo

            // 回写缓存（异步，不阻塞主流程）
            go f.cacheFlowInfo(context.Background(), flowID, flowInfo)
        }
    }

    return result, nil
}

// fetchFlowInfoFromAPI 从 Skylark API 获取 flow 信息
func (f *skylarkFlowRegistry) fetchFlowInfoFromAPI(
    ctx context.Context,
    flowID int64,
) (*FlowInfo, error) {
    // 调用已封装的 GetFlowDetail 接口
    flowDetail, err := f.GetFlowDetail(ctx, "", flowID, "")
    if err != nil {
        return nil, err
    }

    return &FlowInfo{
        ID:    flowDetail.ID,
        Title: flowDetail.Title,
    }, nil
}

// cacheFlowInfo 异步回写缓存
func (f *skylarkFlowRegistry) cacheFlowInfo(
    ctx context.Context,
    flowID int64,
    flowInfo *FlowInfo,
) {
    cacheKey := fmt.Sprintf("skylark:flow:%d", flowID)
    data, err := json.Marshal(flowInfo)
    if err != nil {
        logx.Errorf("序列化 flow 信息失败: %v", err)
        return
    }

    // 缓存 1 小时（flow 信息变化频率低）
    if err := f.cache.Set(ctx, cacheKey, string(data), 3600); err != nil {
        logx.Errorf("缓存 flow 信息失败: %v", err)
    }
}
```

### 2.4 核心领域模型扩展

```go
// core/flow.go
type Assignment struct {
    ID         int64
    AssigneeID int64
    Status     string
    Category   string
    VertexID   int64
    JourneyID  int64
    FlowID     int64   // 新增：流程ID
    FlowTitle  string  // 新增：流程名称
    CreatedAt  string
    UpdatedAt  string
}
```

### 2.5 Engine 层接口

```go
// engine/handler.go

// GetUserAssignments 获取用户任务列表（自动填充 flow_id 和 flow_title）
func (h *handler) GetUserAssignments(
    ctx context.Context,
    app string,
    userID int64,
    category string,
    authHeader string,
    page int,
    pageSize int,
) ([]*core.Assignment, int, error) {
    // Step 1: 调用 Skylark API 获取原始列表
    assignments, total, err := h.flowRegistry.GetUserAssignments(
        ctx, app, userID, category, authHeader, page, pageSize,
    )
    if err != nil {
        return nil, 0, err
    }

    // Step 2: 自动填充 flow_id 和 flow_title
    if err := h.flowRegistry.enrichAssignmentsWithFlowInfo(ctx, assignments); err != nil {
        logx.Errorf("填充 flow 信息失败: %v", err)
        // 不影响主流程，继续返回
    }

    return assignments, total, nil
}
```

### 2.6 性能对比

| 场景 | 优化前 | 优化后（首次） | 优化后（缓存命中） |
|------|--------|---------------|-------------------|
| 100条记录，50个journey，3个flow | 1次列表 + 50次journey查询 + 3次flow查询<br>= 54次API请求 | 1次列表 + 1次DB查询 + 3次flow查询<br>= 4次API请求 + 1次DB | 1次列表 + 1次DB查询<br>= 1次API请求 + 1次DB |
| 耗时估算 | 50×50ms + 3×100ms<br>= 2.8秒 | 1×50ms + 1×10ms + 3×100ms<br>= 360ms | 1×50ms + 1×10ms<br>= 60ms |
| **性能提升** | - | **87%** | **98%** |

### 2.7 配置与依赖

#### 必需配置

```go
// internal/flows/config.go
type Config struct {
    // 数据库连接（用于查询 journey 映射）
    SkylarkDB sqlx.SqlConn

    // 缓存接口
    Cache core.CacheInterface
}

// internal/flows/flows.go
func NewSkylarkFlowRegistry(config *Config, cache core.CacheInterface) (SkylarkFlowRegistry, error) {
    if config.SkylarkDB == nil {
        return nil, errors.New("SkylarkDB 配置不能为空")
    }
    return &skylarkFlowRegistry{
        db:    config.SkylarkDB,
        cache: cache,
    }, nil
}
```

#### Engine 层初始化

```go
// engine/handler.go
func NewHandler(config Config) (Handler, error) {
    // ... 其他 Manager 初始化

    // 初始化 FlowRegistry（传入 Skylark DB 连接）
    flowRegistry, err := flows.NewSkylarkFlowRegistry(&flows.Config{
        SkylarkDB: config.SkylarkDB, // 新增配置项
    }, cacheInterface)
    if err != nil {
        return nil, err
    }

    // ...
}
```

### 2.8 数据库连接说明

**方案A（推荐）**：只读副本连接
```go
// 使用 Skylark PostgreSQL 的只读副本
skylarkDB := sqlx.NewSqlConn("postgres",
    "host=skylark-readonly.db port=5432 user=readonly ...",
)
```

**方案B（备选）**：如果无法访问数据库
```go
// 通过缓存 + API 调用实现
// 首次查询后缓存 journey_id → flow_id 映射（TTL 24小时）
```

### 2.9 错误处理

```go
// 如果映射库查询失败，降级为逐个 API 调用
func (f *skylarkFlowRegistry) enrichAssignmentsWithFlowInfo(
    ctx context.Context,
    assignments []*core.Assignment,
) error {
    // 尝试批量查询
    err := f.batchEnrichFlowInfo(ctx, assignments)
    if err != nil {
        logx.Errorf("批量查询失败，降级为逐个查询: %v", err)
        // 降级方案：逐个查询（保证功能可用）
        return f.fallbackEnrichFlowInfo(ctx, assignments)
    }
    return nil
}
```

---

## 三、待实现接口设计

### 3.1 流程记录搜索 📋

**API**: `POST /api/v4/yaw/flows/:id/journeys/search`

---

### 3.2 获取流程审批历史 📋

**API**: `GET /api/v4/yaw/journeys/:id/moments`

---

### 3.3 获取当前流程任务的处理者 📋

**API**: `GET /api/v4/yaw/flows/:flow_id/journeys/:id/current_processing_users`

---

### 3.4 终止流程任务 📋

**API**: `PUT /api/v4/yaw/flows/:flow_id/journeys/:id`

---

## 四、使用示例

### 已实现功能 ✅

```go
// 获取用户待处理的任务列表（自动补充 flow_id 和 flow_title）
assignments, total, err := engine.GetUserAssignments(
    ctx, "tenant-001", "local-user-123",
    core.AssignmentCategoryProcessed, 1, 20,
)

// 获取用户发起的流程列表
journeys, total, err := engine.GetProposedJourneys(
    ctx, "tenant-001", "local-user-123", 1, 20,
)
```

详细使用示例请参考各接口的文档注释。

---

## 五、实施步骤

### 第1步：Core层定义 ✅ (已完成)
- ✅ 创建 `core/journey.go`，定义 `Journey`、`FlowUser`、`Assignment` 结构体
- ✅ 扩展 `Assignment` 添加 `FlowID` 和 `FlowTitle` 字段（性能优化）
- ✅ 新增 URL 构建器（`BuildUserAssignmentsURL`、`BuildProposedJourneysURL`）

### 第2步：Model层定义 ✅ (已完成)
- ✅ `internal/flows/model.go` 添加响应结构体及 `ToDomain()` 方法
- ✅ 新增 `UserAssignmentsResponse` 和 `ProposedJourneysResponse`

### 第3步：接口定义 ✅ (7/10 完成)
- ✅ 核心接口：`GetJourneyBySN`、`GetJourneyAssignments`、`UpdateJourneyStatus`、`GetJourneyDetail`、`GetFlowDetail`
- ✅ 工作台接口：`GetUserAssignments`、`GetProposedJourneys`
- 📋 待完成：4 个辅助接口

### 第4步：接口实现 ✅ (7/10 完成)
- ✅ 已实现 7 个接口（租户模式 + 集中式验证 + 错误处理 + 模型转换 + 性能优化）
- ✅ 新增 `enrichment.go` 实现性能优化逻辑（215行）
- 📋 待完成：4 个辅助接口实现

### 第5步：Engine层集成 ✅ (7/10 完成)
- ✅ 已集成 7 个对外接口（含本地用户ID自动转换）
- 📋 待完成：4 个辅助接口集成

### 第6步：配置化改造 ✅ (已完成)
- ✅ `internal/flows/config.go` 添加缓存配置（`FlowInfoCacheTTL`）
- ✅ 设置合理默认值（3600秒）
- ✅ 支持自定义配置

### 第6步：错误定义 ✅
错误已在 `core/errors.go` 中定义：
- ✅ `ErrJourneyNotFound` - 已存在并使用

### 第7步：测试 📋 (待实现)
1. 单元测试：使用 `httptest` mock Skylark API 响应
2. 集成测试：测试完整调用链
3. 边界测试：测试空列表、不存在的记录等

### 已验证
- ✅ 代码通过 `go build ./...` 编译验证
- ✅ 遵循三层架构规范
- ✅ Core 层无框架依赖

---

## 六、文件变更清单

| 文件 | 操作 | 状态 | 说明 |
|------|------|------|------|
| `core/address.go` | 修改 | ✅ 已完成 | 新增 2 个 URL 构建器 |
| `core/assignment.go` | 修改 | ✅ 已完成 | 扩展 Assignment 字段（FlowID、FlowTitle）+ 常量 |
| `core/cache.go` | 修改 | ✅ 已完成 | 新增 Flow 信息缓存接口方法 |
| `core/query.go` | 修改 | ✅ 已完成 | 新增缓存键前缀常量 |
| `core/journey.go` | 新增 | ✅ 已完成 | Journey、JourneyDetail、Attachment 领域模型 |
| `core/flow.go` | 修改 | ✅ 已完成 | FlowDetail、FlowField、FlowVertex、FlowEdge 领域模型 |
| `internal/cache/flow.go` | 修改 | ✅ 已完成 | 实现 Flow 缓存方法（GetFlowInfo、SetFlowInfo） |
| `internal/flows/config.go` | 修改 | ✅ 已完成 | 新增缓存配置（FlowInfoCacheTTL，默认3600秒） |
| `internal/flows/flows.go` | 修改 | ✅ 已完成 | 添加 7/10 个接口方法定义 |
| `internal/flows/model.go` | 修改 | ✅ 已完成 | 新增 2 个响应结构体 |
| `internal/flows/handler.go` | 修改 | ✅ 已完成 | 实现 7/10 个接口 + 依赖注入调整 |
| `internal/flows/enrichment.go` | **新增** | ✅ 已完成 | 性能优化核心逻辑（217行） |
| `engine/engine.go` | 修改 | ✅ 已完成 | 添加 7/10 个对外接口方法 |
| `engine/handler.go` | 修改 | ✅ 已完成 | 实现 7/10 个对外接口 + 注入 GetRemoteDB |

**已完成代码量**: ~600 行（包含性能优化 + 配置化改造）
**预计总代码量**: ~1000 行（10个接口 + 架构优化）
**完成进度**: 70%

---

## 七、注意事项

### 架构规范
1. **Core 层纯净**：只使用 Go 原生类型，禁止 `db` 标签、`sql.Null*`
2. **数据流向**：`API → Model → Core → 用户`
3. **错误处理**：使用 `core/var.go` 预定义错误

### API调用规范
1. 使用 `httpc.Do` 发送请求
2. 使用 `httputils.ReadJSONResponse` 解析响应
3. 所有请求添加 `Authorization` 头

### 分页处理
1. 默认 `page=1`, `per_page=20`
2. 返回总数和当前页数据
3. 响应头包含 `X-SLP-Total-Count`、`X-SLP-Total-Pages`

### 缓存策略 ✅
1. **Flow 信息缓存**：通过 `Config.FlowInfoCacheTTL` 配置（默认 3600 秒）
2. 建议范围：1800-7200 秒（30分钟到2小时）
3. 未配置时自动使用默认值（不会报错）
4. 配置示例：
   ```go
   config := &flows.Config{
       FlowInfoCacheTTL: 7200, // 自定义 2 小时
   }
   ```

---

## 八、预期效果与当前进度

### 功能完整性
- ✅ **已完成**: 流程管理核心功能（7个接口：查询、详情、更新、列表）
- ✅ **性能优化**: GetUserAssignments 自动补充 flow_id 和 flow_title（API调用减少93%+）
- 📋 **待实现**: 4 个辅助接口（搜索、审批历史、当前处理人、终止流程）

### 代码质量
- ✅ 遵循三层架构 + 租户模式 + 配置化
- ✅ 完整文档注释 + 规范错误处理
- ✅ Core 层纯净（无框架依赖）
- ✅ 集中式 API 配置验证（EnableAPI 开关）
- ✅ 配置化缓存（支持自定义TTL，默认值合理）
- ✅ 编译验证通过（`go build ./...`）

### 已完成里程碑
- ✅ **核心调用链打通** (2025-11-12)
  - 5个接口：GetJourneyBySN、GetJourneyAssignments、UpdateJourneyStatus、GetJourneyDetail、GetFlowDetail
  - 建立标准实现模式（租户配置 + 错误映射 + 模型转换）
  - 完成架构优化（集中式验证 + SkylarkAPIConfig 轻量级结构）

- ✅ **工作台接口 + 性能优化** (2025-11-12)
  - 2个核心工作台接口：GetUserAssignments、GetProposedJourneys
  - 性能优化：映射库 + 批量查询 + Redis缓存（API调用减少93%+）
  - 配置化改造：FlowInfoCacheTTL 可配置（默认3600秒）
  - 本地用户ID支持：Engine层自动转换为远程用户ID

---

## 九、后续优化建议

1. **批量操作**：支持批量查询 journeyID、批量更新状态
2. **流程图可视化**：基于 Vertices 和 Edges 生成流程图数据
3. **下一节点自动推断**：根据当前节点自动计算 nextVertexID
4. **事件通知**：集成 Webhook 监听流程状态变化
5. **性能优化**：关键接口增加缓存，减少 API 调用次数

---

## 十、下次开发建议

根据优先级，建议按以下顺序实现剩余接口：

### 1️⃣ 高优先级（已完成 ✅）

**核心调用链 + 详情查询** (⭐⭐⭐⭐⭐)
- ✅ GetJourneyBySN：流程编号 → journeyID
- ✅ GetJourneyAssignments：journeyID → assignmentID
- ✅ UpdateJourneyStatus：更新流程状态
- ✅ GetJourneyDetail：获取流程记录详情（包含字段值和附件）
- ✅ GetFlowDetail：获取流程详情（包含字段、节点、边信息）

### 2️⃣ 后续功能（管理工作台支持）

剩余 5 个接口可按需实现，每个接口预计 2-3 小时。

### 💡 开发技巧

- ✅ 复用已有的 HTTP 调用模式
- ✅ 复用已有的错误处理机制
- ✅ 参考 `GetJourneyBySN` 的实现结构
- ✅ 优先实现 Internal 层，测试通过后再暴露到 Engine 层
