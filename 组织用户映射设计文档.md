# 组织/用户映射方案设计文档

## 一、架构概述

### 1.1 租户与Skylark空间映射模型

```
Nexlyn租户 ←→ Skylark平台空间（1:1）
├─ 约束：一个租户只能对接一个Skylark空间
├─ 标识：tenant_id（nexlyn） → platform_config → Skylark空间
└─ 配置：数据库连接 + API认证信息
```

**设计原则**：
- **一租户一平台**：`platform_configs.tenant_id` UNIQUE 约束
- **集中管理**：平台配置存储在 eventhandler 微服务
- **统一认证**：数据库连接和API Token统一存储

### 1.2 模块职责划分

```
internal/
├── organization/          # 组织管理（新建）
│   ├── Skylark 组织 CRUD（调用 REST API）
│   └── 组织ID映射：local_org_id ↔ remote_org_id
│
├── user/                  # 用户管理（新建）
│   ├── Skylark 用户 CRUD（调用 REST API）
│   └── 用户ID映射：local_user_id ↔ remote_user_id
│
└── mapping/               # 业务字段映射（保留）
    └── 业务字段值映射：remote_org_value → local_org_id（用于Query/Stats）
```

**三种映射的区别**：
- **组织ID映射**（新）：管理本地组织ID与Skylark组织ID的双向映射
- **用户ID映射**（新）：管理本地用户ID与Skylark用户ID的双向映射
- **业务字段映射**（旧）：映射远程事件数据中的组织字段值到本地组织ID

---

## 二、Platform配置扩展

### 2.1 新增字段

扩展 `skylark_platform_configs` 表，添加API认证信息：

```sql
ALTER TABLE skylark_platform_configs ADD COLUMN:
  api_base_url VARCHAR(255) NOT NULL,  -- Skylark API地址（如 https://skylark.example.com）
  api_token TEXT NOT NULL;             -- API认证Token（加密存储）
```

**完整表结构**：
```sql
CREATE TABLE skylark_platform_configs (
    id VARCHAR(36) PRIMARY KEY,
    tenant_id VARCHAR(255) UNIQUE NOT NULL,  -- 一租户一平台

    -- 远程数据库连接（用于查询）
    host VARCHAR(255) NOT NULL,
    port INTEGER NOT NULL,
    database VARCHAR(255) NOT NULL,
    username VARCHAR(255) NOT NULL,
    password VARCHAR(255) NOT NULL,
    namespace_id INTEGER NOT NULL,

    -- API认证（用于写操作）
    api_base_url VARCHAR(255) NOT NULL,
    api_token TEXT NOT NULL,

    created_by VARCHAR(255),
    updated_by VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2.2 Core层领域模型扩展

```go
// core/platform.go
type PlatformConfig struct {
    ID          string
    TenantID    string

    // 数据库连接信息
    Host        string
    Port        int
    Database    string
    Username    string
    Password    string
    NamespaceID int

    // API认证信息（新增）
    APIBaseURL  string    // API基础地址
    APIToken    string    // 认证Token

    CreatedBy   *string
    UpdatedBy   *string
    CreatedAt   time.Time
    UpdatedAt   time.Time
}
```

---

## 三、映射表设计

### 3.1 组织映射表

```sql
CREATE TABLE skylark_org_mappings (
    id VARCHAR(36) PRIMARY KEY,
    tenant_id VARCHAR(255) NOT NULL,
    local_org_id VARCHAR(255) NOT NULL,      -- 本地组织ID
    remote_org_id INT NOT NULL,              -- Skylark组织ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(tenant_id, local_org_id),         -- 本地ID唯一
    UNIQUE(tenant_id, remote_org_id)         -- 远程ID唯一
);

CREATE INDEX idx_org_mapping_tenant ON skylark_org_mappings(tenant_id);
```

### 3.2 用户映射表

```sql
CREATE TABLE skylark_user_mappings (
    id VARCHAR(36) PRIMARY KEY,
    tenant_id VARCHAR(255) NOT NULL,
    local_user_id VARCHAR(255) NOT NULL,     -- 本地用户ID
    remote_user_id INT NOT NULL,             -- Skylark用户ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(tenant_id, local_user_id),        -- 本地ID唯一
    UNIQUE(tenant_id, remote_user_id)        -- 远程ID唯一
);

CREATE INDEX idx_user_mapping_tenant ON skylark_user_mappings(tenant_id);
```

**约束说明**：
- 双向唯一约束：确保本地ID和远程ID一一对应
- 租户隔离：所有查询必须带 `tenant_id` 条件

---

## 四、Manager接口设计

### 4.1 Organization Manager

```go
// 组织操作（7个方法）
CreateOrganization(ctx, tenantID, localOrgID, name, description string, founderID int) (*core.Organization, error)
CreateSubOrganization(ctx, tenantID, localOrgID, parentLocalOrgID, name, description string, founderID int) (*core.Organization, error)
DeleteOrganization(ctx, tenantID, localOrgID string) error
GetOrganization(ctx, tenantID, localOrgID string) (*core.Organization, error)
ListOrganizations(ctx, tenantID string) ([]*core.Organization, error)
ListRootOrganizations(ctx, tenantID string) ([]*core.Organization, error)
ListChildOrganizations(ctx, tenantID, parentLocalOrgID string) ([]*core.Organization, error)

// 映射查询（内部使用）
GetRemoteOrgID(ctx, tenantID, localOrgID string) (int, error)
GetRemoteOrgIDs(ctx, tenantID string, localOrgIDs []string) ([]int, error)
GetLocalOrgID(ctx, tenantID string, remoteOrgID int) (string, error)
```

**核心逻辑**：
- 创建组织：调用Skylark API → 保存映射 → 更新缓存
- 删除组织：调用Skylark API → 删除映射 → 清理缓存
- 查询映射：优先缓存 → 数据库查询 → 回写缓存

### 4.2 User Manager

```go
// 用户操作（3个方法）
CreateUser(ctx, tenantID, localUserID, name string, identifier, phone, openid *string) (*core.User, error)
GetUser(ctx, tenantID, localUserID string) (*core.User, error)
ListUsers(ctx, tenantID string) ([]*core.User, error)

// 映射查询（内部使用）
GetRemoteUserID(ctx, tenantID, localUserID string) (int, error)
GetRemoteUserIDs(ctx, tenantID string, localUserIDs []string) ([]int, error)
GetLocalUserID(ctx, tenantID string, remoteUserID int) (string, error)
```

---

## 五、Core层领域模型

### 5.1 组织模型

```go
// core/organization.go
type Organization struct {
    ID       int     // Skylark组织ID
    ParentID *int    // 父组织ID（可空）
    Ancestry *string // 祖先路径（可空，如"14516/15113"）
}

type OrgIDMapping struct {
    ID          string
    TenantID    string
    LocalOrgID  string
    RemoteOrgID int
    CreatedAt   time.Time
    UpdatedAt   time.Time
}

// 函数注入类型
type GetPlatformConfigFunc func(ctx context.Context, tenantID string) (*PlatformConfig, error)
type GetRemoteOrgIDsFunc func(ctx context.Context, tenantID string, localOrgIDs []string) ([]int, error)
```

**设计说明**：
- 只存储Skylark层级关系（ID、ParentID、Ancestry）
- 不存储name、description等本地信息（由nexlyn的system_organizations管理）
- 用户-组织关系由本地system_user_org_relations表管理

### 5.2 用户模型

```go
// core/user.go
type User struct {
    ID int // Skylark用户ID
}

type UserIDMapping struct {
    ID           string
    TenantID     string
    LocalUserID  string
    RemoteUserID int
    CreatedAt    time.Time
    UpdatedAt    time.Time
}

// 函数注入类型
type GetRemoteUserIDsFunc func(ctx context.Context, tenantID string, localUserIDs []string) ([]int, error)
```

### 5.3 错误定义

```go
// core/errors.go
// 组织相关错误
ErrOrgNotFound       = errors.New("organization not found in Skylark")
ErrOrgCreateFailed   = errors.New("failed to create organization in Skylark")
ErrOrgDeleteFailed   = errors.New("failed to delete organization in Skylark")
ErrParentOrgNotFound = errors.New("parent organization mapping not found")

// 用户相关错误
ErrUserNotFound        = errors.New("user not found in Skylark")
ErrUserMappingNotFound = errors.New("user mapping not found")
ErrUserMappingExists   = errors.New("user mapping already exists")
ErrUserCreateFailed    = errors.New("failed to create user in Skylark")

// Skylark API错误
ErrSkylarkAPIUnauthorized = errors.New("Skylark API unauthorized")
ErrSkylarkAPINotFound     = errors.New("Skylark API resource not found")
ErrSkylarkAPIBadRequest   = errors.New("Skylark API bad request")
ErrSkylarkAPIServerError  = errors.New("Skylark API server error")
```

---

## 六、gRPC接口设计

### 6.1 服务定义

```protobuf
// api/proto/skylark_mapping.proto
service SkylarkMappingService {
  // 组织映射
  rpc CreateOrgMapping(CreateOrgMappingRequest) returns (OrgMappingResponse);
  rpc GetOrgMapping(GetOrgMappingRequest) returns (OrgMappingResponse);
  rpc DeleteOrgMapping(DeleteOrgMappingRequest) returns (DeleteOrgMappingResponse);

  // 用户映射
  rpc CreateUserMapping(CreateUserMappingRequest) returns (UserMappingResponse);
  rpc GetUserMapping(GetUserMappingRequest) returns (UserMappingResponse);
  rpc DeleteUserMapping(DeleteUserMappingRequest) returns (DeleteUserMappingResponse);
}

message CreateOrgMappingRequest {
  string tenant_id = 1;
  string local_org_id = 2;
  string name = 3;
  string description = 4;
  int32 founder_id = 5;           // Skylark用户ID
  string parent_local_org_id = 6; // 可选，创建子组织时使用
}

message OrgMappingResponse {
  string local_org_id = 1;
  int32 remote_org_id = 2;
  int32 parent_id = 3;           // 可空
  string ancestry = 4;           // 可空
}

message CreateUserMappingRequest {
  string tenant_id = 1;
  string local_user_id = 2;
  string name = 3;
  string identifier = 4;         // 可空
  string phone = 5;              // 可空
  string openid = 6;             // 可空
}

message UserMappingResponse {
  string local_user_id = 1;
  int32 remote_user_id = 2;
}
```

### 6.2 调用流程

```
Backend创建组织流程:
1. Backend接收HTTP请求（创建组织）
2. Backend开启本地事务
3. Backend插入system_organizations表
4. Backend通过gRPC调用eventhandler.CreateOrgMapping()
   ├─ eventhandler调用Skylark REST API创建远程组织
   ├─ eventhandler保存映射关系到skylark_org_mappings表
   └─ eventhandler返回remote_org_id
5. Backend完成本地事务
6. Backend返回HTTP响应
```

**关键设计**：
- **同步创建**：在同一HTTP请求内完成本地创建和远程映射
- **事务分离**：backend和eventhandler各自管理自己的事务
- **失败处理**：如果gRPC调用失败，backend回滚本地事务
- **幂等性**：重复调用检查映射是否已存在，避免重复创建

---

## 七、依赖关系与初始化

### 7.1 Engine层初始化顺序

```
Cache → Platform → Organization → User → Flows/Forms → Mapping → Event → Query/Stats
```

**关键依赖**：
- Organization/User依赖Platform的`GetPlatformConfigFunc`（获取APIBaseURL、APIToken）
- Query/Stats依赖Platform的`GetRemoteDB`（获取远程数据库连接）
- Query/Stats依赖Mapping的`ListOrgMappings`（组织权限过滤）

### 7.2 函数注入示例

```go
// engine/handler.go
platformMgr := platform.NewManager(platformConfig)

orgMgr := organization.NewManager(organization.Config{
    LocalDB:           localDB,
    Cache:             cache,
    GetPlatformConfig: platformMgr.GetConfig, // 函数注入
})

userMgr := user.NewManager(user.Config{
    LocalDB:           localDB,
    Cache:             cache,
    GetPlatformConfig: platformMgr.GetConfig, // 函数注入
})
```

---

## 八、缓存策略

### 8.1 映射缓存

| 类型 | 缓存键 | TTL | 说明 |
|------|--------|-----|------|
| 组织ID映射 | `skylark:org_mapping:{tenant}:{local_org_id}` | 30天 | 正向映射 |
| 组织反向映射 | `skylark:org_mapping_rev:{tenant}:{remote_org_id}` | 30天 | 反向映射 |
| 用户ID映射 | `skylark:user_mapping:{tenant}:{local_user_id}` | 30天 | 正向映射 |
| 用户反向映射 | `skylark:user_mapping_rev:{tenant}:{remote_user_id}` | 30天 | 反向映射 |

### 8.2 批量查询优化

```
GetRemoteOrgIDs批量查询逻辑:
1. 遍历localOrgIDs，优先从缓存获取
2. 收集缓存未命中的localOrgIDs
3. 批量查询数据库（IN子句）
4. 回写缓存
5. 返回合并结果
```

**性能优势**：
- 减少数据库查询次数（批量查询）
- 提高缓存命中率（30天TTL）
- 支持高并发场景

---

## 九、与Nexlyn项目集成

### 9.1 Nexlyn架构概览

```
微服务架构:
├─ backend（端口8888/9999）
│  ├─ 用户管理（system_users）
│  ├─ 组织管理（system_organizations）
│  └─ 用户-组织关系（system_user_org_relations）
│
└─ eventhandler（端口8888/9999）
   ├─ 平台配置（skylark_platform_configs）
   ├─ 事件配置（event_configs）
   ├─ 组织映射（skylark_org_mappings）- 新增
   └─ 用户映射（skylark_user_mappings）- 新增

数据共享:
├─ 共享PostgreSQL数据库（nexlyn）
└─ 共享Redis缓存
```

### 9.2 集成场景

#### 场景1：创建组织时同步创建Skylark组织

```
Backend逻辑:
1. 接收创建组织请求
2. 开启本地事务
3. 插入system_organizations表（生成local_org_id）
4. gRPC调用eventhandler.CreateOrgMapping(tenant_id, local_org_id, name, desc, founder_id)
5. 提交本地事务
```

#### 场景2：创建用户时同步创建Skylark用户

```
Backend逻辑:
1. 接收创建用户请求
2. 开启本地事务
3. 插入system_users表（生成local_user_id）
4. gRPC调用eventhandler.CreateUserMapping(tenant_id, local_user_id, name, phone)
5. 提交本地事务
```

#### 场景3：查询事件时过滤组织权限

```
Eventhandler逻辑（Query/Stats）:
1. 从JWT Token获取用户local_user_id
2. 查询system_user_org_relations获取用户的local_org_ids
3. 调用organization.GetRemoteOrgIDs()获取remote_org_ids
4. 查询event_org_mappings获取remote_org_values
5. 构建SQL: WHERE org_field = ANY($remote_org_values)
```

---

## 十、实施步骤

### 第一阶段：Core层基础（✅已完成）
- ✅ 创建 core/organization.go（组织领域模型）
- ✅ 创建 core/user.go（用户领域模型）
- ✅ 扩展 core/errors.go（组织/用户/API错误定义）
- ✅ 扩展 core/platform.go（APIBaseURL和APIToken字段）

### 第二阶段：Platform配置扩展 + 数据库初始化（✅已完成）
- ✅ 更新 internal/platform/model.go（添加API认证字段）
- ✅ 更新 internal/platform/handler.go（支持API字段CRUD + InitTable方法）
- ✅ 创建所有sql.go（platform、event、mapping、organization、user）
- ✅ SQL完全基于 nexlyn-deploy/008-event-handler-schema.sql，保证兼容性
- ✅ 新增表：skylark_org_mappings、skylark_user_mappings

### 第三阶段：实现Organization Manager
- 创建internal/organization包（5个文件）
- 实现Skylark组织API调用
- 实现映射管理逻辑
- 集成到Engine层

### 第四阶段：实现User Manager
- 创建internal/user包（5个文件）
- 实现Skylark用户API调用
- 实现映射管理逻辑
- 集成到Engine层

### 第五阶段：gRPC接口
- 定义proto文件
- 实现gRPC服务端（eventhandler）
- 实现gRPC客户端（backend）
- 集成测试

### 第六阶段：测试与文档
- 单元测试（organization、user）
- 集成测试（gRPC调用）
- 更新CLAUDE.md、DEVELOPMENT.md、README.md

---

## 十一、注意事项

### 11.1 组织创建顺序

**必须先创建用户**：
```
正确顺序: 创建用户（获取Skylark用户ID）→ 创建组织（使用用户ID作为founder_id）
```

### 11.2 映射唯一性

**双向唯一约束**：
- 一个本地组织/用户只能映射一个远程组织/用户
- 一个远程组织/用户只能映射一个本地组织/用户
- 重复创建返回`ErrOrgMappingExists`或`ErrUserMappingExists`

### 11.3 事务处理

**Backend事务**：
- backend管理system_users、system_organizations的事务
- gRPC调用失败时，backend回滚本地事务

**Eventhandler事务**：
- eventhandler管理skylark_org_mappings、skylark_user_mappings的事务
- API调用失败时，不保存映射关系

### 11.4 错误处理

**gRPC调用失败**：
- backend检查错误类型，决定是否重试
- 幂等性保证：重复调用检查映射是否已存在

**Skylark API失败**：
- 401 Unauthorized → 检查API Token配置
- 404 Not Found → 父组织不存在
- 500 Server Error → 记录日志，稍后重试

---

## 十二、核心价值

### 12.1 清晰的职责划分
- **Organization Manager**：组织CRUD + 组织ID映射
- **User Manager**：用户CRUD + 用户ID映射
- **Mapping Manager**：业务字段值映射（保留）

### 12.2 自动化映射管理
- 创建组织/用户时自动创建映射
- 删除组织/用户时自动删除映射
- 无需手动管理映射关系

### 12.3 高性能缓存策略
- 映射关系缓存30天
- 批量查询优化
- 减少数据库访问

### 12.4 符合DDD架构
- 三层架构（Engine → Internal → Core）
- 函数注入解耦（避免循环依赖）
- 数据模型与领域模型分离
- 事务保证数据一致性

---

## 十三、参考架构图

```
Nexlyn项目架构:
┌─────────────────────────────────────────────────────────────┐
│                        Backend微服务                        │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────┐  │
│  │  system_users  │  │system_orgs     │  │user_org_rels │  │
│  └────────────────┘  └────────────────┘  └──────────────┘  │
│         │                    │                    │         │
│         └────────────────────┼────────────────────┘         │
│                              │ gRPC调用                     │
└──────────────────────────────┼──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                     Eventhandler微服务                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              go-skylark引擎（pkg/skylarkq）          │  │
│  │  ┌──────────┐  ┌──────────┐  ┌─────────┐  ┌───────┐ │  │
│  │  │Platform  │→│Organization│→│  User   │→│Mapping│ │  │
│  │  │ Manager  │  │  Manager   │  │ Manager │  │Manager│ │  │
│  │  └──────────┘  └──────────┘  └─────────┘  └───────┘ │  │
│  │       ↓              ↓             ↓            ↓     │  │
│  │  ┌─────────────────────────────────────────────────┐ │  │
│  │  │  platform_   org_       user_      event_org_  │ │  │
│  │  │  configs     mappings   mappings   mappings    │ │  │
│  │  └─────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│                              │                              │
│                              │ REST API / PostgreSQL查询    │
└──────────────────────────────┼──────────────────────────────┘
                               │
                               ▼
                      ┌──────────────────┐
                      │  Skylark平台     │
                      │  - organizations │
                      │  - users         │
                      │  - flows         │
                      │  - assignments   │
                      └──────────────────┘
```

**数据流向**：
1. **创建流程**：Backend（HTTP） → Backend（本地DB事务） → gRPC → Eventhandler（Skylark API + 映射表） → Skylark平台
2. **查询流程**：Eventhandler（查询请求） → 映射转换（local_org_id → remote_org_value） → 远程PostgreSQL查询 → 数据返回

---

## 十四、详细开发阶段规划

### 阶段1：扩展 Platform 配置（2-3小时）

**目标**：支持 API 认证信息存储

**任务清单**：
- [x] 更新 core/platform.go（添加 APIBaseURL、APIToken 字段）
- [ ] 更新 internal/platform/model.go（PlatformConfigModel 添加对应字段）
- [ ] 更新 internal/platform/handler.go（Create/Update 方法处理新字段）
- [ ] 数据库迁移脚本（ALTER TABLE 添加两个字段）
- [ ] 测试平台配置的创建和查询

**验证标准**：
- 创建平台配置时可以保存 API 认证信息
- 查询平台配置时可以获取 API 认证信息
- 所有现有测试通过

---

### 阶段2：实现 Organization Manager（1-2天）

**目标**：实现组织管理和映射功能

**任务清单**：

**1. 创建包结构**
- [ ] internal/organization/organization.go（Manager 接口定义）
- [ ] internal/organization/config.go（Config 结构体）
- [ ] internal/organization/model.go（OrgIDMappingModel + ToDomain）
- [ ] internal/organization/handler.go（Manager 实现）
- [ ] internal/organization/helpers.go（HTTP 客户端封装）

**2. 实现核心功能**
- [ ] Skylark 组织 API 调用（POST/DELETE/GET /api/v4/organizations）
- [ ] 映射管理（创建、查询、删除）
- [ ] 缓存策略（30天 TTL）
- [ ] 批量查询优化（GetRemoteOrgIDs）

**3. 集成到 Engine 层**
- [ ] 更新 engine/handler.go（初始化 Organization Manager）
- [ ] 更新 engine/engine.go（添加 7 个组织接口）

**4. 数据库和测试**
- [ ] 提供 skylark_org_mappings 表创建脚本
- [ ] 单元测试（CreateOrganization、DeleteOrganization、批量查询）

**验证标准**：
- 可以创建 Skylark 组织并自动保存映射
- 可以通过 local_org_id 查询 remote_org_id
- 批量查询性能达标（缓存命中率 > 80%）
- 单元测试覆盖率 > 80%

---

### 阶段3：实现 User Manager（1天）

**目标**：实现用户管理和映射功能

**任务清单**：

**1. 创建包结构**（参考 Organization）
- [ ] internal/user/user.go（Manager 接口定义）
- [ ] internal/user/config.go（Config 结构体）
- [ ] internal/user/model.go（UserIDMappingModel + ToDomain）
- [ ] internal/user/handler.go（Manager 实现）
- [ ] internal/user/helpers.go（可复用 Organization 的 HTTP 客户端）

**2. 实现核心功能**
- [ ] Skylark 用户 API 调用（POST/GET /api/v4/users）
- [ ] 映射管理（创建、查询、删除）
- [ ] 缓存策略（30天 TTL）
- [ ] 批量查询优化（GetRemoteUserIDs）

**3. 集成到 Engine 层**
- [ ] 更新 engine/handler.go（初始化 User Manager）
- [ ] 更新 engine/engine.go（添加 3 个用户接口）

**4. 数据库和测试**
- [ ] 提供 skylark_user_mappings 表创建脚本
- [ ] 单元测试（CreateUser、GetUser、批量查询）

**验证标准**：
- 可以创建 Skylark 用户并自动保存映射
- 可以通过 local_user_id 查询 remote_user_id
- 单元测试覆盖率 > 80%

---

### 阶段4：gRPC 接口实现（1-2天）

**目标**：为 nexlyn 项目提供 gRPC 接口

**任务清单**：

**1. Proto 定义**
- [ ] 定义 skylark_mapping.proto（6 个 RPC 方法）
  - CreateOrgMapping / GetOrgMapping / DeleteOrgMapping
  - CreateUserMapping / GetUserMapping / DeleteUserMapping
- [ ] 定义请求和响应消息结构

**2. gRPC 服务端（eventhandler 项目）**
- [ ] 生成 gRPC 服务端代码
- [ ] 实现 SkylarkMappingService（调用 go-skylark Engine）
- [ ] 错误处理和状态码映射
- [ ] 日志和监控

**3. gRPC 客户端（backend 项目）**
- [ ] 生成 gRPC 客户端代码
- [ ] 封装调用逻辑
- [ ] 超时和重试策略
- [ ] 连接池管理

**4. 集成测试**
- [ ] Backend → gRPC → Eventhandler → Skylark 完整链路测试
- [ ] 事务失败场景测试（gRPC 调用失败时 Backend 回滚）
- [ ] 幂等性测试（重复调用不创建重复映射）

**验证标准**：
- Backend 可以通过 gRPC 创建组织/用户映射
- gRPC 调用失败时 Backend 可以正确回滚事务
- 重复调用不会创建重复映射
- 端到端测试通过

---

### 阶段5：文档和测试（半天）

**目标**：完善文档和测试

**任务清单**：

**1. 更新项目文档**
- [ ] CLAUDE.md
  - 新增 Organization/User Manager 架构说明
  - 更新初始化顺序
  - 新增接口清单（从 15 个增加到 25 个）
- [ ] DEVELOPMENT.md
  - 新增组织/用户映射开发指引
  - 新增 Skylark API 调用规范
  - 新增映射管理最佳实践
- [ ] README.md
  - 更新接口列表（新增 10 个接口）
  - 新增组织/用户管理使用示例
  - 新增数据库表创建说明

**2. 集成测试**
- [ ] 端到端测试（创建组织 → 创建用户 → 查询映射 → 删除）
- [ ] 性能测试（批量查询 1000 个映射）
- [ ] 并发测试（100 个并发创建请求）

**3. 代码审查**
- [ ] 运行 go fmt ./...
- [ ] 运行 go vet ./...
- [ ] 检查依赖方向（禁止 Core 依赖 Internal）
- [ ] 检查错误处理（使用预定义错误）

**验证标准**：
- 文档完整准确，可指导新开发者
- 所有测试通过（单元测试 + 集成测试）
- 代码符合项目规范（CLAUDE.md 要求）

---

## 十五、开发时间估算

| 阶段 | 预计时间 | 依赖 | 状态 |
|------|---------|------|------|
| 阶段1：Platform 配置扩展 | 2-3小时 | 无 | 🚧 进行中 |
| 阶段2：Organization Manager | 1-2天 | 阶段1 | 📋 待开始 |
| 阶段3：User Manager | 1天 | 阶段1 | 📋 待开始 |
| 阶段4：gRPC 接口 | 1-2天 | 阶段2、阶段3 | 📋 待开始 |
| 阶段5：文档和测试 | 半天 | 阶段4 | 📋 待开始 |
| **总计** | **4-6天** | - | - |

**关键路径**：阶段1 → 阶段2 → 阶段4 → 阶段5

**并行机会**：
- 阶段2（Organization）和阶段3（User）可以部分并行
- helpers.go（HTTP 客户端）可以共享，先在 Organization 中实现

---

## 十六、风险管理

### 风险1：Skylark API 文档不完整

**影响**：API 调用失败，字段不匹配
**概率**：中
**缓解措施**：
- 先抓包分析现有 flows/forms 模块的 API 调用
- 获取真实请求格式（Header、Body、认证方式）
- 编写 API 调试工具，快速验证接口

### 风险2：gRPC 接口设计变更

**影响**：需要重新生成代码，影响 Backend 集成
**概率**：低
**缓解措施**：
- 在阶段4开始前与 Backend 开发者确认接口设计
- 使用 Proto 版本控制（如 v1、v2）
- 保留向后兼容性

### 风险3：批量查询性能问题

**影响**：查询映射时性能不达标，影响用户体验
**概率**：低
**缓解措施**：
- 使用 Redis 缓存（30天 TTL）
- 批量查询数据库（IN 子句，避免 N+1 查询）
- 性能测试验证（目标：1000 个映射查询 < 100ms）

### 风险4：事务一致性问题

**影响**：Backend 和 Eventhandler 事务不一致，数据不同步
**概率**：中
**缓解措施**：
- gRPC 调用失败时 Backend 必须回滚本地事务
- 实现幂等性（重复调用检查映射是否已存在）
- 编写集成测试覆盖失败场景

---

## 十七、关键设计决策记录

### 决策1：一租户一平台（2025-11-10）

**背景**：需要确定租户与 Skylark 空间的映射关系
**决策**：采用一对一映射（tenant_id UNIQUE 约束）
**理由**：
- 简化架构设计
- 连接池管理简单（以 tenant_id 为键）
- 符合当前业务场景

### 决策2：API Token 存储在 platform_configs 表（2025-11-10）

**背景**：需要存储 Skylark API 认证信息
**决策**：扩展 platform_configs 表，添加 api_base_url 和 api_token 字段
**理由**：
- 统一管理平台配置（数据库连接 + API 认证）
- 避免引入新表
- 数据库连接和 API 调用通常对应同一个平台

### 决策3：同步创建映射（2025-11-10）

**背景**：确定映射创建时机
**决策**：Backend 创建本地用户/组织时，通过 gRPC 同步调用 Eventhandler 创建映射
**理由**：
- 保证数据一致性（在同一个 HTTP 请求内完成）
- 简化逻辑（不需要延迟创建或手动映射）
- 错误处理清晰（gRPC 失败时 Backend 回滚事务）

### 决策4：gRPC 通信（2025-11-10）

**背景**：Backend 和 Eventhandler 之间的通信方式
**决策**：使用 gRPC 接口
**理由**：
- 类型安全（Proto 定义明确）
- 高性能（二进制协议）
- 与现有架构一致（Backend 和 Eventhandler 已有 gRPC 端口）

---

## 十八、后续优化方向

### 优化1：支持一租户多平台

**场景**：一个租户需要对接多个 Skylark 空间
**改动**：
- 移除 tenant_id UNIQUE 约束
- 修改连接池键为 (tenantID, platformID)
- 事件配置增加 platform_config_id 字段

### 优化2：异步创建映射

**场景**：同步创建映射影响性能
**改动**：
- Backend 发布事件到消息队列
- Eventhandler 异步消费事件并创建映射
- 增加映射状态字段（pending、completed、failed）

### 优化3：缓存预热

**场景**：首次查询映射时缓存未命中
**改动**：
- 服务启动时批量加载映射到 Redis
- 定时任务刷新缓存（避免过期）

### 优化4：监控和告警

**场景**：需要监控 API 调用成功率
**改动**：
- 集成 Prometheus 指标
- 监控 API 调用延迟、成功率、错误率
- 设置告警阈值（如成功率 < 95%）
