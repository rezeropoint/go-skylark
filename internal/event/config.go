package event

/*
Package event 提供事件+字段配置管理功能

# 关键注意事项

1. **聚合管理**：事件配置和字段配置作为一个整体管理，使用数据库事务保证原子性

2. **完整替换策略**：更新字段配置时先删除所有旧字段，再插入新字段，避免复杂的 diff 逻辑

3. **依赖注入**：通过 core.GetRemoteDBFunc 函数注入方式依赖 Platform Manager，避免循环依赖

4. **唯一性约束**：
   - 事件名称在租户内唯一
   - flow_id 在租户内唯一
   - 字段名在事件配置内唯一

5. **级联删除**：删除事件配置时，字段配置自动级联删除（ON DELETE CASCADE）
*/
