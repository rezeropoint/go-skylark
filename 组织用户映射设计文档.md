# 组织/用户映射方案设计文档

## 📊 项目进度概览

| 阶段 | 状态 | 完成时间 |
|------|------|---------|
| 阶段1：Core层基础 | ✅ 已完成 | - |
| 阶段2：Platform配置扩展 | ✅ 已完成 | - |
| 阶段3：Organization Manager | ✅ 已完成 | - |
| 阶段4：User Manager | ✅ 已完成 | 2025-11-11 |
| 阶段5：架构优化 | ✅ 已完成 | 2025-01-17 |
| 阶段6：测试与文档 | ✅ 已完成 | 2025-01-18 |

**当前进度**：6/6 阶段完成（100%）✅

**说明**：本项目是 Go SDK 包，不提供 gRPC 接口。调用方直接引入 go-skylark 包使用。

**已实现功能**：
- ✅ 组织CRUD + ID映射（4个接口：CreateOrganization、CreateSubOrganization、DeleteOrganization、UpdateOrganization）
- ✅ 组织成员管理（2个接口：AddMembers、RemoveMembers）
- ✅ 用户CRUD + ID映射（1个接口：CreateUser）
- ✅ 同步状态检查（2个接口：GetOrgSyncStatus、GetUserSyncStatus）
- ✅ **映射表校验机制**：修改操作前校验远程成员是否都在本地映射表中，确保同步正常 ✨
- ✅ 双向缓存策略（正向+反向，TTL 30天；成员列表TTL 5分钟）
- ✅ 批量查询优化（缓存优先 → 数据库批量查询）
- ✅ HTTP工具包（统一错误处理 + 响应解析）
- ✅ 架构优化：封装远程ID，使用零值而非指针（符合 Go 习惯）
- ✅ **接口总数**：9个（组织7个 + 用户2个）

**代码质量验证**：
- ✅ `go build ./...` 构建通过
- ✅ `go fmt ./...` 格式化完成
- ✅ `go vet ./...` 静态分析通过

---

## 一、架构概述

### 1.1 租户与Skylark空间映射模型

```
Nexlyn租户 ←→ Skylark平台空间（1:1）
├─ 约束：一个租户只能对接一个Skylark空间
├─ 标识：tenant_id（nexlyn） → platform_config → Skylark空间
└─ 配置：数据库连接 + API认证信息
```

**设计原则**：
- **一租户一平台**：`platform_configs.tenant_id` UNIQUE 约束
- **集中管理**：平台配置存储在 eventhandler 微服务
- **统一认证**：数据库连接和API Token统一存储

### 1.2 模块职责划分

```
internal/
├── organization/          # 组织管理（✅ 已实现）
│   ├── Skylark 组织 CRUD（调用 REST API）
│   └── 组织ID映射：local_org_id ↔ remote_org_id
│
├── user/                  # 用户管理（✅ 已实现）
│   ├── Skylark 用户 CRUD（调用 REST API）
│   └── 用户ID映射：local_user_id ↔ remote_user_id
│
├── httputils/             # HTTP工具包（✅ 已实现）
│   ├── 统一HTTP错误处理
│   └── JSON响应解析
│
└── mapping/               # 业务字段映射（保留）
    └── 业务字段值映射：remote_org_value → local_org_id（用于Query/Stats）
```

**三种映射的区别**：
- **组织ID映射**（✅ 已实现）：管理本地组织ID与Skylark组织ID的双向映射
- **用户ID映射**（✅ 已实现）：管理本地用户ID与Skylark用户ID的双向映射
- **业务字段映射**（保留）：映射远程事件数据中的组织字段值到本地组织ID

---

## 二、Platform配置扩展（✅ 已完成）

扩展 `skylark_platform_configs` 表，新增字段：
- `api_base_url`：Skylark API 地址
- `api_token`：API 认证 Token

**用途**：支持组织和用户的 CRUD 操作（调用 Skylark REST API）

---

## 三、映射表设计（✅ 已完成）

### 3.1 组织映射表
- 表名：`skylark_org_mappings`
- 映射关系：`local_org_id ↔ remote_org_id`
- 约束：租户隔离 + 双向唯一

### 3.2 用户映射表
- 表名：`skylark_user_mappings`
- 映射关系：`local_user_id ↔ remote_user_id`
- 约束：租户隔离 + 双向唯一

---

## 四、AdminEngine 接口设计（✅ 已完成）

### 4.1 Organization Manager（7个接口）
- **组织CRUD**：CreateOrganization、CreateSubOrganization、DeleteOrganization、UpdateOrganization
- **成员管理**：AddMembers、RemoveMembers（内部使用 GetMembers 校验）
- **同步状态**：GetOrgSyncStatus

### 4.2 User Manager（2个接口）
- **用户CRUD**：CreateUser
- **同步状态**：GetUserSyncStatus

**设计原则**：
- ✅ 封装远程ID（对使用者透明）
- ✅ 自动映射管理（SDK内部维护）
- ✅ 使用零值而非指针（符合 Go 习惯）
- ✅ **映射表校验**：修改操作前验证同步状态

---

## 五、核心特性（✅ 已完成）

### 5.1 映射表校验机制 ✨
在 AddMembers、RemoveMembers、UpdateOrganization 操作前：
1. 调用 GetMembers 获取远程组织的当前成员
2. 通过 FillLocalUserIDMap 批量校验映射表
3. 确保所有远程成员都在本地映射表中
4. 若有未同步成员则返回错误，保证数据一致性

### 5.2 双向ID映射
- 组织映射：`local_org_id ↔ remote_org_id`
- 用户映射：`local_user_id ↔ remote_user_id`
- 缓存策略：Redis 缓存（TTL 30天）+ 批量查询优化

### 5.3 缓存策略
| 类型 | TTL | 说明 |
|------|-----|------|
| 组织/用户ID映射 | 30天 | 双向映射 |
| 组织成员列表 | 5分钟 | 支持子孙组织 |

---

## 六、使用示例（✅ 已完成）

### 基本流程
1. 引入 SDK：`import "github.com/rezeropoint/go-skylark/admin"`
2. 初始化 AdminEngine
3. 创建用户并自动生成映射
4. 创建组织（founder自动成为管理员）并自动生成映射
5. 将用户加入组织

详细代码示例请参考第十二节"核心价值"。

---

## 七、已缓解的风险（✅ 已完成）

- ✅ Skylark API 调用：HTTP 客户端封装 + 统一错误处理
- ✅ 批量查询性能：双向缓存（TTL 30天）+ 批量查询优化
- ✅ 架构解耦：函数注入避免循环依赖

---

## 八、实施完成（✅ 2025-01-18）

### 完成内容
- ✅ Platform 配置扩展（API 认证字段）
- ✅ Organization Manager（组织CRUD + 成员管理 + 管理员管理）
- ✅ User Manager（用户CRUD + 同步状态检查）
- ✅ 双向缓存策略（ID映射 + 成员/管理员列表）
- ✅ HTTP工具包（统一错误处理）
- ✅ 架构优化（封装远程ID + 移除重复接口）

### 代码质量
- ✅ `go build ./...` 构建通过
- ✅ `go fmt ./...` 格式化完成
- ✅ `go vet ./...` 静态分析通过

---

## 九、实施步骤

### ✅ 阶段1-4：基础功能实现（已完成）

已完成内容概览：
- ✅ **Core层基础**：领域模型定义 + 错误类型扩展
- ✅ **Platform配置扩展**：API认证字段（api_base_url, api_token）
- ✅ **Organization Manager**：组织CRUD + ID映射 + 双向缓存（7个文件，3个Engine接口）
- ✅ **User Manager**：用户CRUD + ID映射 + 双向缓存（6个文件，2个Engine接口）
- ✅ **通用工具包**：internal/httputils（HTTP错误处理 + 响应解析）

核心特性：
- 函数注入解耦（避免循环依赖）
- 双向缓存（正向+反向映射，TTL 30天）
- 批量查询优化（缓存优先 → 数据库批量查询）
- 数据库表自动初始化
- 完整错误处理（使用预定义错误类型）

代码质量验证：
- ✅ `go build ./...` 构建通过
- ✅ `go fmt ./...` 格式化完成
- ✅ `go vet ./...` 静态分析通过

### ✅ 阶段5：架构优化（已完成 2025-01-17）

**重构目标**：移除远程ID的不合理暴露，提升封装性

**修改内容**：
- ✅ User模块接口重构
  - `CreateUser`: `(*core.User, error)` → `error`
  - 删除 `GetUser` 方法（无实际用途）
  - 保留内部方法（GetRemoteUserID、GetRemoteUserIDs、FillLocalUserIDMap）

- ✅ Organization模块接口重构
  - `CreateOrganization`: `(*core.Organization, error)` → `error`
  - `CreateSubOrganization`: `(*core.Organization, error)` → `error`
  - 保留内部方法（GetRemoteOrgID、GetRemoteOrgIDs、GetLocalOrgID）

- ✅ AdminEngine接口同步更新
  - 所有创建方法只返回 error
  - 删除 GetUser 接口

- ✅ Core层领域模型注释更新
  - User/Organization 标注"内部使用"
  - 强调远程ID对使用者透明

**改进价值**：
- ✅ 符合封装原则（远程ID完全内部化）
- ✅ 简化接口（不返回无用数据）
- ✅ 降低耦合（使用者只需关心本地ID）
- ✅ 代码质量：编译通过，无破坏性变更

### 📋 阶段6：测试与文档（✅ 已完成 2025-01-18）
- ✅ 移除 GetMembers 接口的公开暴露（内部方法用于校验）
- ✅ 实现映射表校验机制（AddMembers、RemoveMembers、UpdateOrganization）
- ✅ 文件结构规范（未导出方法移至 internal.go）
- ✅ UpdateOrganizationRequest 重构（使用零值而非指针）
- ✅ 更新架构文档

---

## 十、注意事项

### 10.1 组织创建顺序

**必须先创建用户**：
```
正确顺序: 创建用户（获取Skylark用户ID）→ 创建组织（使用用户ID作为founder_id）
```

### 10.2 映射唯一性

**双向唯一约束**：
- 一个本地组织/用户只能映射一个远程组织/用户
- 一个远程组织/用户只能映射一个本地组织/用户
- 重复创建返回`ErrOrgMappingExists`或`ErrUserMappingExists`

### 10.3 错误处理

**Skylark API失败**：
- 401 Unauthorized → 检查API Token配置
- 404 Not Found → 父组织不存在
- 500 Server Error → 记录日志，稍后重试

### 10.4 调用方事务管理

调用方应在创建本地数据后调用 SDK，失败时需回滚本地事务。

---

## 十一、核心价值

### 11.1 清晰的职责划分
- **Organization Manager**：组织CRUD + 组织ID映射 + 同步状态检查
- **User Manager**：用户CRUD + 用户ID映射 + 同步状态检查
- **Mapping Manager**：业务字段值映射（保留）

### 11.2 自动化映射管理
- 创建组织/用户时自动创建映射
- 删除组织/用户时自动删除映射
- 无需手动管理映射关系

### 11.3 高性能缓存策略
- 映射关系缓存30天
- 批量查询优化
- 减少数据库访问

### 11.4 符合DDD架构
- 三层架构（Engine → Internal → Core）
- 函数注入解耦（避免循环依赖）
- 数据模型与领域模型分离
- 事务保证数据一致性

### 11.5 良好的封装性（✨ 架构改进 2025-01-17）
- ✅ **远程ID封装**：远程用户ID/组织ID完全是SDK内部实现细节，对使用者透明
- ✅ **简化接口**：CreateUser/CreateOrganization 只返回 error，不暴露无用数据
- ✅ **降低耦合**：使用者只需关心本地ID，无需了解 Skylark 平台内部细节
- ✅ **保持一致**：函数注入类型（GetRemoteUserIDsFunc等）保持不变，内部依赖不受影响
- ✅ **符合原则**：符合信息隐藏原则和最小知识原则

**改进对比**：重构前返回包含远程ID的对象，重构后只返回error，映射关系SDK内部自动管理。

---

## 十二、风险管理

### ✅ 已缓解风险

- ✅ **Skylark API调用**：已实现HTTP客户端封装（internal/httputils + organization/user helpers.go）
- ✅ **批量查询性能**：已实现双向缓存（TTL 30天）+ 批量查询优化
- ✅ **架构解耦**：已通过函数注入避免循环依赖

### 待关注风险

**风险1：事务一致性问题**
- **影响**：调用方和 SDK 的事务不一致，数据不同步
- **缓解**：调用方在 SDK 调用失败时必须回滚本地事务，实现幂等性检查

**风险2：Skylark API 限流**
- **影响**：大量并发创建请求可能触发限流
- **缓解**：实现请求队列和重试机制

---

## 十三、已解决的设计问题 ✅

### 问题1：单管理员约束 vs 多管理员设计（已解决 2025-01-18）

#### 背景
**Skylark API 设计**：
- 支持一个组织有多个管理员
- 提供完整的管理员 CRUD 接口（添加、修改、删除、查询）
- 创建组织时 founder 自动成为管理员（access.id=2, "创建者"权限）

**本地业务需求**（从 `nexlyn/restful/backend` 分析）：
- 一个组织只有一个管理员（`manager_id` 字段）
- 通过 `UpdateOrganization` 接口修改管理员
- 管理员是组织属性的一部分，而非独立实体

#### 最终决策：采用"更新组织"模式 ✨

**决策内容**：
- ❌ 移除 4 个独立的管理员接口（GetAdministrators、AddAdministrator、UpdateAdministrator、DeleteAdministrator）
- ✅ 新增 `UpdateOrganization` 接口，支持更新组织的所有属性（包括管理员）
- ✅ SDK 内部封装管理员更换的复杂逻辑（查询旧管理员 → 添加新管理员 → 删除旧管理员）
- ✅ 使用分布式锁（Redis）确保管理员更新的原子性和并发安全

**实现方案**：

```go
// 1. 新增接口
func UpdateOrganization(ctx context.Context, req *UpdateOrganizationRequest) error

// 2. 请求参数（所有字段可选）
type UpdateOrganizationRequest struct {
    TenantID   string // 租户ID（必需）
    LocalOrgID string // 本地组织ID（必需）
    Name       string // 组织名称（可选，空字符串表示不修改）
    ManagerID  string // 管理员的本地用户ID（可选，空字符串表示不修改管理员）
    // 注意：管理员权限ID固定为1（"管理员"权限），不提供参数
}

// 3. 内部实现（封装多次 API 调用）
// - 获取分布式锁（防止并发修改冲突）
// - 查询当前所有管理员
// - 添加新管理员（Skylark API）
// - 删除所有旧管理员（Skylark API，多次调用）
// - 释放分布式锁
// - 更新基本信息（如果指定 Name 或 Description）
```

**决策理由**：

1. ✅ **与本地业务接口完全对齐**：
   - 本地业务通过 `UpdateOrganization` 修改 `manager_id`
   - SDK 接口设计与本地业务模型一致

2. ✅ **简化使用**：
   - 一次调用完成所有更新（包括管理员）
   - 无需理解 Skylark 的多管理员模型

3. ✅ **封装复杂性**：
   - SDK 内部处理多次 API 调用
   - 使用分布式锁确保原子性

4. ✅ **并发安全**：
   - Redis 分布式锁（SETNX + TTL 30秒）
   - 避免多个请求同时修改管理员导致数据不一致

5. ✅ **避免误用**：
   - 不暴露多管理员接口，防止"添加第二个管理员"的错误
   - 更换管理员是原子操作，无中间状态

**接口数量变化**：
- 移除前：13个接口（3个组织CRUD + 3个成员管理 + 4个管理员管理 + 2个用户管理 + 1个同步状态）
- 移除后：10个接口（移除4个管理员接口，新增1个 UpdateOrganization）

**技术细节**：

| 方面 | 实现 |
|------|------|
| 分布式锁键 | `skylark:lock:org_manager:{tenantID}:{localOrgID}` |
| 锁超时时间 | 30秒 |
| 锁实现 | `cache.AcquireLock()` / `cache.ReleaseLock()` |
| 冲突错误 | `ErrOrganizationManagerUpdateConflict` |
| 管理员权限ID | 固定为1（"管理员"权限），不提供参数 |

**使用示例**：

```go
// 场景1：只更换管理员（权限ID固定为1）
req := &core.UpdateOrganizationRequest{
    TenantID:   "tenant_001",
    LocalOrgID: "org_456",
    ManagerID:  "user_123", // 空字符串表示不修改管理员
}
err := adminEngine.UpdateOrganization(ctx, req)

// 场景2：同时更新名称和管理员
req := &core.UpdateOrganizationRequest{
    TenantID:   "tenant_001",
    LocalOrgID: "org_456",
    Name:       "新组织名称", // 空字符串表示不修改名称
    ManagerID:  "user_789",
}
err := adminEngine.UpdateOrganization(ctx, req)
```

**优势总结**：
- ✅ 符合本地业务需求（单管理员模型）
- ✅ 封装 Skylark 复杂性（内部处理多次 API 调用）
- ✅ 并发安全（分布式锁）
- ✅ 接口简洁（一次调用完成）
- ✅ 语义清晰（更新组织 vs 管理员 CRUD）

---

## 十四、后续优化方向

### 优化1：支持一租户多平台

**场景**：一个租户需要对接多个 Skylark 空间
**改动**：
- 移除 tenant_id UNIQUE 约束
- 修改连接池键为 (tenantID, platformID)
- 事件配置增加 platform_config_id 字段

### 优化2：缓存预热

**场景**：首次查询映射时缓存未命中
**改动**：
- 服务启动时批量加载映射到 Redis
- 定时任务刷新缓存（避免过期）

### 优化3：监控和告警

**场景**：需要监控 API 调用成功率
**改动**：
- 集成 Prometheus 指标
- 监控 API 调用延迟、成功率、错误率
- 设置告警阈值（如成功率 < 95%）
